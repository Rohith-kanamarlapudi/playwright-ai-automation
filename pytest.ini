[pytest]
testpaths =
    tests
    test_llm.py
    test_prompts.py

asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

markers =
    slow: marks tests as slow
    smoke: marks tests as smoke tests

# To run ONLY generated tests (not the default):
# pytest generated_tests/ -v